
const arr1 = ['A', 'H', 'M', 'A', 'D'];

const arr2 = ['F', 'A', 'R', 'A', 'Z'];

const arr3 = arr1.concat(arr2);

// ------------------------

let Check_Name=arr3.forEach(function (Elt)  {
})

console.log(arr3.join(' '));

// ----------------------------

Check_Name=arr1.forEach((Elt) => {}) ;

console.log( arr1.join(' '));
Check_Name=arr2.forEach((Elt) => {});
console.log(arr2.join(' '));